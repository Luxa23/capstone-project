import styled from 'styled-components';

const StyledTextHome = styled.p`
  font-family: var(--font-family);
  color: #999999;
  margin: 0;
  padding: 0 2rem 0 3.5rem;
  align-self: start;
`;

export default StyledTextHome;
